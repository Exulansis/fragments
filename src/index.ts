import { getUserInput } from "./utils"

/*
 * User asked for a random digit 10 times
 *
 * Evaluate how random the input is
 * (o) Compare against a random number generator
 * Print out the results
*/

async function runProgram () {
  /*
   * Function to receive user input
   * const userInput = await getUserInput('Please provide your age')
  */
}

runProgram()